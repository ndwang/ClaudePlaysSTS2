[project]
name = "sts2client"
version = "0.1.0"
description = "LLM-powered client for the STS2 agent mod"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "requests",
]

[project.scripts]
sts2client = "client:run"
